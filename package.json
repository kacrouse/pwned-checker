{
  "name": "go-nimbly-tech-challenge",
  "version": "1.0.0",
  "description": "An interface for the Have I Been Pwned API.",
  "scripts": {
    "start": "node server",
    "start:dev": "concurrently \"npm start\" \"cd client && npm start\"",
    "build": "cd client/ && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Crouse",
  "license": "ISC",
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^1.0.3"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "dotenv": "^8.2.0"
  }
}
